name: Build Windows AIFX Player

on:
  workflow_dispatch:

jobs:
  build-windows:
    runs-on: windows-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        shell: pwsh
        run: |
          python -m pip install --upgrade pip
          pip install pyinstaller flask werkzeug jinja2 click itsdangerous markupsafe

      - name: Clean build folders
        shell: pwsh
        run: |
          if (Test-Path "build") { Remove-Item -Recurse -Force "build" }
          if (Test-Path "dist")  { Remove-Item -Recurse -Force "dist" }

      - name: Build with PyInstaller
        shell: pwsh
        run: |
          pyinstaller --clean --noconfirm paion-player.spec

      - name: Inspect dist output
        shell: pwsh
        run: |
          Write-Host "=== DIST CONTENTS ==="
          if (Test-Path "dist") {
            Get-ChildItem -Recurse "dist" | Select-Object FullName
          } else {
            Write-Host "ERROR: dist folder not found"
            exit 1
          }

      - name: Zip Windows build
        shell: pwsh
        run: |
          if (Test-Path "dist\\AIFX-Player") {
            Compress-Archive `
              -Path "dist\\AIFX-Player\\*" `
              -DestinationPath "AIFX-Player-Windows.zip" `
              -Force
          }
          elseif (Test-Path "dist\\AIFX-Player.exe") {
            Compress-Archive `
              -Path "dist\\AIFX-Player.exe" `
              -DestinationPath "AIFX-Player-Windows.zip" `
              -Force
          }
          else {
            Write-Host "ERROR: Expected AIFX-Player output not found"
            Get-ChildItem -Recurse "dist" | Select-Object FullName
            exit 1
          }

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: AIFX-Player-Windows
          path: AIFX-Player-Windows.zip
